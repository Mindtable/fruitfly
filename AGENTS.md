# Project instructions

## Commit messages

- Use `v{version}` as the commit-message header, replacing `{version}` with
  the current value from `build.gradle.kts`.
- Follow the header with a blank line and a description written as a list of
  bullet points.
